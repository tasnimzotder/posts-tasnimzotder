---
title: Git & GitHub — Open a Pull Request [As fast as possible]
seoTitle: Git & GitHub — Open a Pull Request [As fast as possible]
abstract: So, what is git? A confusing one, isn’t it? Off course yes, until we learn the basics.
isPublished: true
publishedOn: 2020-10-12T00:00:00-0530
layout: Article
cover: "./../assets/images/1-cover.jpg"
tags:
    - GitHub
    - Programming
---

## Git - Understanding the Basics

> Git is a version control system for tracking changes in files and coordinating work among multiple people.

- Git is a free and open source distributed version control system designed to handle everything from small to very large projects with speed and efficiency.
- Git is a distributed revision control system, meaning that changes are stored in a central repository and can be accessed from any computer in the world.
- Git is a free and open source distributed version control system designed to handle everything from small to very large projects with speed and efficiency.

## Install Git

### 1. On Windows

1. Download the Git for Windows installer from [GitHub]()
2. Install the Git for Windows installer

### 2. On Linux

1. Open a terminal and type

```bash
sudo apt update
sudo apt install git
```

## Configure Git

Open a terminal and type,

```bash
git config --global user.name "<YOUR_NAME>"
git config --global user.email "<YOUR_EMAIL>"
```

To verify that your Git configuration is correct, open a terminal and type,

```bash
git config --list
```

This will show you the current configuration.

```bash
user.name = "<YOUR_NAME>"
user.email = "<YOUR_EMAIL>"
```

<MxCalloutCard
type={'default'}
message={'GitHub is a code hosting service for version control using Git.'}
/>

## Steps to make a Pull Request

### 1. Select a repo on GitHub

There are lots of repos on GitHub. You can search and find the repo you want. Choose the repo you want to pull requests from.

### 2. Fork the repo to your own account

<MxImage alt="Fork" src="./../assets/images/1-fork.jpg" orientation="horizontal" size="half" />

### 3. Clone the repo to your local machine

<MxImage alt="Clone" src="./../assets/images/1-clone.jpg" orientation="horizontal" size="half" />

Copy the `HTTPS` clone link and paste it in your terminal. The link is like `https://github.com/<USERNAME>/<REPO_NAME>.git`

```bash
git clone <COPIED_LINK>
```

### 4. Open the repo in a code editor (for example, VS Code)

```
code <REPO_NAME>
```

### 5. Change, Add, and Commit your changes

Make some changes to the repo. After that, add all the changes you made to the repo and commit them.

```bash
git add .
git commit -m "Added some changes"
```

Push your changes to the remote repo.

```bash
git push origin main
```

### 6. Open a pull request

<MxImage alt="initiate PR" src="./../assets/images/1-initiate-pr.jpg" orientation="horizontal" size="full" />

<MxImage alt="confirm the changes" src="./../assets/images/1-confirm-the-changes.jpg" orientation="horizontal" size="full" />

<MxImage alt="PR description" src="./../assets/images/1-pr-description.jpg" orientation="horizontal" size="full" />
